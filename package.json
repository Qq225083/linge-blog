{
  "name": "linge-blog",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && echo 'Deploy to GitHub Pages via: git push origin main'",
    "deploy:gh": "vite build && npx gh-pages -d dist -m 'deploy: auto publish [ci skip]'"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.4.0"
  }
}
